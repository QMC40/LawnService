@model int;

@if (Model > 0)
{
    <li class="nav-item" font-size="1.3rem">
        <a class="nav-link text-dark" asp-controller="Orders" asp-action="ShoppingCart">
            <i class="bi bi-cart4" style="font-size:1.6rem; color: white"></i> @Model
        </a>
    </li>
}